<UserControl x:Class="AppSwitcher.UI.Controls.KeyAssignmentButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="35"
             d:DesignHeight="35"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             x:Name="Root">
    <ui:Button
        x:Name="InnerButton"
        Content="{Binding ButtonContent, ElementName=Root, FallbackValue=A}"
        Appearance="{Binding ButtonAppearance, ElementName=Root, FallbackValue=Secondary}"
        Width="30"
        Height="30"
        Padding="1"
        MinWidth="0"
        Focusable="True"
        RenderTransformOrigin="0.5,0.5"
        Click="Button_Click"
        PreviewKeyDown="Button_PreviewKeyDown"
        LostKeyboardFocus="Button_LostKeyboardFocus">
        <ui:Button.RenderTransform>
            <ScaleTransform x:Name="ButtonScaleTransform" ScaleX="1" ScaleY="1" />
        </ui:Button.RenderTransform>
    </ui:Button>
</UserControl>
