<UserControl x:Class="AppSwitcher.UI.Controls.ModifierKeySelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             d:DesignHeight="100" d:DesignWidth="500">
    <UserControl.Resources>
        <Style x:Key="ModifierButtonStyle" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <Label Content="Modifier Key:"
               VerticalAlignment="Center"
               Margin="0,0,10,0"/>
        <ui:Button x:Name="RightAltButton"
                   Content="Right Alt"
                   Click="ModifierButton_Click"
                   Tag="RightAlt"
                   Style="{StaticResource ModifierButtonStyle}" />
        <ui:Button x:Name="RightCtrlButton"
                   Content="Right Ctrl"
                   Click="ModifierButton_Click"
                   Tag="RightCtrl"
                   Style="{StaticResource ModifierButtonStyle}" />
        <ui:Button x:Name="RightShiftButton"
                   Content="Right Shift"
                   Click="ModifierButton_Click"
                   Tag="RightShift"
                   Style="{StaticResource ModifierButtonStyle}" />
        <ui:Button x:Name="AppsButton"
                   Content="Apps"
                   Click="ModifierButton_Click"
                   Tag="Apps">
            <ui:Button.Icon>
                <ui:SymbolIcon Symbol="CalendarAgenda24"/>
            </ui:Button.Icon>
        </ui:Button>
    </StackPanel>
</UserControl>
